file(GLOB SOURCE_FILES *.c)

file(GLOB HEADER_FILES ${CMAKE_SOURCE_DIR}/include/*.h)

add_library(vkzos ${SOURCE_FILES} ${HEADER_FILES})

target_include_directories(vkzos PUBLIC ${CONAN_INCLUDE_DIRS} ${CMAKE_SOURCE_DIR}/include)
target_link_libraries(vkzos PUBLIC ${CONAN_LIBS})
